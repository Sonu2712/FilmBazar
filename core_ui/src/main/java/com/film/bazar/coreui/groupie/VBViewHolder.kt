package com.film.bazar.coreui.groupie

import androidx.viewbinding.ViewBinding
import com.xwray.groupie.GroupieViewHolder

class VBViewHolder<T : ViewBinding>(val binding: T) : GroupieViewHolder(binding.root)
